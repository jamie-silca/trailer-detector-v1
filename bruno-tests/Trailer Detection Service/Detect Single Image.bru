meta {
  name: Detect Single Image
  type: http
  seq: 1
}

post {
  url: http://localhost:8000/detect
  body: multipartForm
  auth: inherit
}

body:multipart-form {
  file: @file()
}

settings {
  encodeUrl: true
}
